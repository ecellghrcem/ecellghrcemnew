/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'aceternity.com'], // Add aceternity.com to the list
    },
};

export default nextConfig; // Use ES module export syntax
